# Database
DB_HOST=DB_HOST
DB_PORT=DB_PORT
DB_NAME=DB_NAME
DB_USER=DB_USER
DB_PASSWORD=DB_PASSWORD

# Server
PORT=PORT
NODE_ENV=NODE_ENV
# Полный URL API (для ссылок на загруженные файлы), например https://api.example.com
BASE_URL=http://localhost:3000

# JWT
JWT_SECRET=JWT_SECRET
JWT_EXPIRES_IN=JWT_EXPIRES_IN